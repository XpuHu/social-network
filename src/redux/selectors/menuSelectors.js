export const selectMainMenu = (state) => {
   return state.sidebar.menu.mainMenu;
}

export const selectUsersMenu = (state) => {
   return state.sidebar.menu.usersMenu;
}

export const selectSettingsMenu = (state) => {
   return state.sidebar.menu.settingsMenu;
}
